# Production Dependencies Only (No Testing Tools)
# Optimized for packaging with PyInstaller

# Core FastAPI
fastapi>=0.104.1
uvicorn[standard]>=0.24.0

# Database
sqlalchemy>=2.0.23
alembic>=1.13.0

# Authentication & Security
python-jose[cryptography]>=3.3.0
pyjwt>=2.8.0
passlib>=1.7.4
bcrypt>=4.0.0

# File Handling
python-multipart>=0.0.6
aiofiles>=23.2.1

# Data Validation
pydantic[email]>=2.10.0
pydantic-settings>=2.1.0

# Email Service
resend>=0.8.0

# QR Code Generation
qrcode[pil]>=7.4.2
pillow>=10.0.0

# Utilities
python-slugify>=8.0.1

# Excel Support (for bulk client upload)
openpyxl==3.1.2
pandas>=2.2.0

# Note: Testing dependencies (pytest, httpx, pytest-asyncio, pytest-cov) 
# are excluded from production build to reduce size
